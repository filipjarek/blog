{% for post in posts %}

	<div class="bg-gradient-to-r from-slate-100 to-yellow-50 h-full"> 
	    <div class="container mx-auto flex flex-wrap py-6">
		    <section class="w-full md:w-2/3 flex flex-col items-center px-1">

			    <article class="flex flex-col ">
				    <div class="bg-white flex flex-col justify-start p-6 shadow hover:shadow-xl">
					    <a href="{{ path('app_category', {'slug': post.category.slug}) }}" class="text-blue-700 text-sm font-bold uppercase pb-4">{{ post.category.name }}</a>
					    <a href="{{ path('app_post', {'slug': post.slug}) }}" class="text-3xl font-bold hover:text-gray-700 pb-4">{{ post.title }}</a>
					    <p class="text-sm pb-3">
						    Published on <b>{{ post.createdAt|format_date('medium') }}</b>
					    </p>
					    <p class="pb-6">Lorem ipsum dolor sit amet, consectetur adipiscing elit. Vivamus quis porta dui. Ut eu iaculis massa. Sed ornare ligula lacus, quis iaculis dui porta volutpat. In sit amet posuere magna..</p>
					    <a href="{{ path('app_post', {'slug': post.slug}) }}" class="uppercase text-gray-800 hover:text-black">Continue Reading
						</a>
				    </div>
			    </article>
		
            </section>
      	</div>
    </div>

{% endfor %}
