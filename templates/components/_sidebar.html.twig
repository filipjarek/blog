<aside class="w-full md:w-1/3 flex flex-col items-center px-3">

	<div class="bg-white w-full shadow flex flex-col my-4 p-6 border border-gray-300">
		<p class="text-sm font-semibold uppercase pb-5"><i class="fa-solid fa-address-card"></i> About Us</p>
		<p class="pb-2 text-justify">Lorem ipsum dolor sit amet, consectetur adipiscing elit. Maecenas mattis est eu odio sagittis tristique. Vestibulum ut finibus leo. In hac habitasse platea dictumst.</p>
		<p class="w-full font-bold form-group group relative flex text-sm uppercase border-2 border-gray-400 hover:border-violet-800 text-gray-400 hover:text-violet-800 hover:text-violet-800flex items-center justify-center px-2 py-3 mt-4">
			Get to know us
		</p>
	</div>

	<div class="bg-white w-full shadow flex flex-col my-4 p-6 border border-gray-300">
		<p class="text-sm font-semibold uppercase pb-5"><i class="fa-solid fa-pen-to-square"></i> Recent Posts</p>

		{% for recentPost in recentPosts %}

			<div class="w-full text-left p-2 border-y border-dashed border-gray-400 hover:underline">
				<p>
					<a href="{{ path('app_post', {'slug': recentPost.slug}) }}" class="card-text">{{ recentPost.title }}</a>
				</p>
			</div>

		{% endfor %}

	</div>

	<div class="bg-white w-full shadow flex flex-col my-4 p-6 border border-gray-300">
		<p class="text-sm font-semibold uppercase pb-5"><i class="fa-solid fa-tags"></i> Tags /{{ tags|length }}/</p>

		<div class="grid grid-cols-3 gap-1 items-center">

			{% for tag in tags %}

				<a href="{{ path('app_tag', {'slug': tag.slug}) }}" class="text-gray-400 hover:text-white hover:bg-violet-800 text-xs font-medium  px-2.5 py-0.5 border border-gray-400 hover:border-violet-800 uppercase">
					<i class="fa-solid fa-tag"></i>
					{{ tag.name }}
				</a>

			{% endfor %}

		</div>

	</div>

</aside>
